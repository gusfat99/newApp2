import { StyleSheet } from "react-native";
export default StyleSheet.create({
    container : {
        flex: 1,
        marginHorizontal: 12,
    },
    inputContainer : {
        flexDirection: 'row',
        justifyContent:'space-between',
    }
});